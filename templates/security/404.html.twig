{% extends '_themes/sneat/base.html.twig' %}

{% block title %}Página no encontrada{% endblock %}

{% block bottom_styles %}
    <link rel="stylesheet" href="{{ asset('build/sneat/vendor/css/pages/page-misc.css') }}" />
{% endblock %}

{% block body %}
    <!-- Error -->
    <div class="container-xxl container-p-y">
        <div class="misc-wrapper">
            <h2 class="mb-2 mx-2 mb-3 text-primary">Página no encontrada :(</h2>
            <p class="mb-4 mx-2">Vaya 😖 No hemos podido encontrar la página que buscas.</p>
            {% set referer = app.request.headers.get('referer')|default('/') %}
            {% if referer == '/' %}
                <a href="{{ path('app_index') }}" class="btn btn-primary mb-3">Volver al inicio</a>
            {% else %}
                <a href="{{ referer }}" class="btn btn-primary mb-3">Volver</a>
            {% endif %}
            <div class="mt-3">
                <img
                        src="{{ asset('assets/img/error.png') }}"
                        alt="page-misc-error-light"
                        width="500"
                        class="img-fluid"
                />

            </div>
        </div>
    </div>
    <!-- /Error -->
{% endblock %}