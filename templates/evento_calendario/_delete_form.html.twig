<form method="post" action="{{ path('app_evento_calendario_delete', {'id': evento_calendario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evento_calendario.id) }}">
    <button class="btn">Delete</button>
</form>
