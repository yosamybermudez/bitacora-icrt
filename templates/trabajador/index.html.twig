{% extends '_themes/sneat/base.html.twig' %}

{% set title = 'Trabajadores' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="card mb-4">
        <div class="card-body d-flex">
            <h4 class="fw-bold mb-0 align-self-center">Trabajadores</h4>
            <ul class="nav nav-pills flex-column flex-md-row-reverse ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_trabajador_new') }}">Nuevo</a>
                </li>
            </ul>
        </div>
    </div>
    {{ include('trabajador/index_table.html.twig') }}


{% endblock %}